apiVersion: v1
kind: Service
metadata:
  name: myapp-lb
  namespace: myapp
spec:
  type: LoadBalancer
  selector:
    app: myapp
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
